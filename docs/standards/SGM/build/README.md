# Build Artifacts Directory

This directory is intended to temporarily hold intermediary or localized build artifacts specific to the SGM standard generation (like localized PDFs). 

**Note**: Final HTML site artifacts for the entire repository are generated at the root `site/` folder, not here.
